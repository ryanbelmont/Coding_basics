Range:
  for i in range(start, stop, step)

Lists:
  Ordered, changeable, heterogeneous (multiple datatypes), allow duplicates.
  prepend: mylist.instert(0, x)
  append: mylist.append(x)
  declare: mylist = []
  length = len(mylist)

dictionary:
  my_dict = {'alex':5, 'ben':10}
  my_dict['ryan'] = 15
  if key in my_dic.keys:
    print("value =", my_dic[key])
